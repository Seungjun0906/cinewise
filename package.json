{
  "dependencies": {
    "@nestjs/mapped-types": "*"
  },
  "name": "cinewise",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "frontMain": "pnpm --filter @repo/cinewise-client",
    "apiMain": "pnpm --filter @repo/cinewise-api",
    "ui": "pnpm --filter @repo/ui"
  },
  "devDependencies": {
    "prettier": "^3.4.2",
    "ts-loader": "^9.4.3",
    "turbo": "^2.3.4",
    "typescript": "5.7.3",
    "@tailwindcss/postcss": "^4.0.5"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  }
}
